NEW TEST FILE: SLS1_ONLY_TEST.PCG
==================================

WHAT IT IS:
-----------
- Updates ONLY SLS1 (new format)
- Leaves SBK1 (old format) UNCHANGED
- First setlist name changed to "SLS1 ONLY TEST"

WHAT'S IN THE FILE:
-------------------
SLS1 (offset 3744): "SLS1 ONLY TEST" (updated)
SBK1 (offset 531920): "NIGHTWISH LEGACY" (unchanged)

WHY THIS SHOULD WORK:
---------------------
The original file has mismatched names:
- SLS1: "MODIFIED SETLIST"
- SBK1: "NIGHTWISH LEGACY"

And it loads fine! So the Kronos accepts mismatched names.

Our problem was CHANGING the SBK1 name, not having mismatched names.

EXPECTED RESULT:
----------------
✓ File should load successfully
✓ First setlist should show "SLS1 ONLY TEST"
✓ All slots should work normally

IF THIS WORKS:
--------------
→ We found the solution!
→ Only update SLS1, never touch SBK1
→ Parser reads from SLS1, so names will display correctly
→ SBK1 stays unchanged, so file validation passes

IF THIS FAILS:
--------------
→ Even SLS1-only updates break something
→ Need to investigate what else is affected
→ May need to look at checksums or other validation

TEST THIS FILE NEXT!
====================
